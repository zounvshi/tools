# 升级版 AI 工具集平台 · 小白部署指南

> 目标：把你手里的「升级代码」变成线上能用的网站。
> 你只需要做几件「只能你本人做」的事，其余代码我已经写好了、也测过了。
> **重大好消息：运行时不再需要任何 GitHub 令牌（PAT）**——且**你原来的 GitHub 数据完全不用动**。
> 新架构：① 原始 8 个工具**仍在 GitHub 仓库，只读**，网站每次实时读取；② 之后所有「新增 / 审批 / 删除」都走你已开通的 Cloudflare R2 桶。
> 你之前卡住的「GitHub 令牌二次确认」问题已经不存在了。你只需要用 GitHub Desktop 把代码推到仓库（走你自己的账号登录）。

---

## 先搞懂：总共要准备哪几样东西

| 东西 | 是什么 | 谁来做 |
|---|---|---|
| 升级代码文件 | 我给你的那一堆文件 | 已给好 |
| `AUTH_SECRET` 密钥 | 给网站签名用的「锁」，我替你生成了 | ✅ 已生成，直接复制 |
| 管理员密码 + 哈希 | 你登录后台用的密码（只存哈希，不存明文） | **你**（用我给的 gen-admin.html） |
| R2 桶绑定 + 公开网址 | 数据 / 上传文件存放处（你已开通 R2） | **你**（Cloudflare 后台点几下） |
| 把代码推到仓库 | 用 GitHub Desktop 把代码放进 `zounvshi/tools` | **你**（用自己账号登录，不用令牌） |
| Cloudflare 环境变量 | 把上面几项填进后台 | **你** |

> ⚠️ 前提：你（或仓库主人）要对 GitHub 仓库 `zounvshi/tools` 有「写权限」，才能用 GitHub Desktop 把代码推上去。
> 如果 `zounvshi` 不是你本人，请让仓库主人给你写权限，或者把代码推到你自己的仓库（见文末 FAQ）。

---

## 第一步：生成管理员密码哈希（决定你能不能登后台）

1. 双击打开我给你的 **`gen-admin.html`**（就在同一文件夹里，用浏览器打开，纯本地工具）。
2. 用户名已经是 `liyt1`，密码框里**自己定一个密码**（记到小本本上，丢了只能重设）。
3. 点「生成 ADMIN_CREDENTIALS」。
4. 下面会出现一整行中括号开头的内容，**点一下它就复制**。
5. 粘贴到《部署配置.txt》第【2】行 `ADMIN_CREDENTIALS=` 后面。

> 为什么不直接填密码？因为网站只存「密码指纹（哈希）」，不存密码本身，更安全。这个小工具在你的浏览器本地算，密码不会上网。

---

## 第二步：把 R2 桶绑到网站（你已开通 R2，这步必做）

> 你已经开通了 R2 并开启了 Public Development URL。现在要把桶「绑」到你的网站项目上，并填两个地址。

1. 打开 https://dash.cloudflare.com → 左侧 **R2** → 找到你建的桶 → 进桶 → **Settings** → 左侧菜单点 **Public Development URL** → 点 **Enable** 并确认。启用后会显示一个公开网址，形如 `https://pub-xxxx.r2.dev`，**复制它**（这就是 `R2_PUBLIC_BASE`）。
   - 若你的界面显示的是「Public access（公开访问）」字样（旧版界面），是同一个功能，同样操作。
2. 回到你的 Pages 项目（`tools-2ks`）→ **Settings** → **Functions** → **R2 bucket bindings** → **Add binding**：
   - Variable name **必须填 `TOOLS_BUCKET`**（代码里写死了这个名字，填错就找不到桶）；
   - Bucket 选刚才建的桶 → 保存。
3. 在项目 **Environment variables** 里新增两条（也可在第四步一并填）：
   - `R2_PUBLIC_BASE` = 第 1 步复制的公开网址（结尾不要带 `/`）；
   - `MAX_UPLOAD_MB` = `100`（单次上传上限，单位 MB）。

> 🔒 安全说明：已通过审核的工具才会把下载地址展示在页面上；未通过的不会放出地址，且管理员「驳回 / 删除」时会自动把对应文件删掉。

---

## 第三步：把代码放进 GitHub 仓库（用 GitHub Desktop，不用令牌）

> GitHub Desktop 是用你自己的账号登录的，**不需要手动令牌、也不会弹那个二次确认**。只有「手动生成 PAT」才会弹。

1. 下载安装 GitHub Desktop：https://desktop.github.com （一路下一步，用你的 GitHub 账号登录）。
2. 打开它 → **File → Clone repository** → 选 `zounvshi/tools` → 选个本地文件夹 → Clone。
3. 把我给你的「升级代码」文件夹里的内容，复制到刚才克隆下来的仓库文件夹里：
   - **覆盖** `index.html`、`tools.json`（旧数据已备份到 git 历史，随时能回滚）
   - **新增** `_headers`、`pending.json`、`functions/` 整个目录
   - 结构必须保持：仓库根目录要有 `index.html`、`_headers`、`tools.json`、`pending.json`，以及 `functions/api/...`
4. GitHub Desktop 左侧会列出改动 → 顶部 Summary 写「升级：权限+审批+实时上线」→ 点 **Commit to main** → 再点 **Push origin**（推送到网上）。
5. 如果你之前 Cloudflare 是「连 GitHub 自动部署」的，推送后它会**自动重新部署**，等几分钟即可。

---

## 第四步：在 Cloudflare 填配置（环境变量）

1. 打开 https://dash.cloudflare.com → 左侧 **Workers & Pages** → 你的项目 `tools-2ks`。
2. 进项目 → **Settings** → **Environment variables**。
3. 点 **Add variable**，按《部署配置.txt》逐行添加（名字 = 等号左边，值 = 等号右边）：
   - `AUTH_SECRET` → 第【1】行的值
   - `ADMIN_CREDENTIALS` → 第【2】行的值（整行中括号内容）
   - `R2_PUBLIC_BASE` → 第二步复制的 R2 公开网址（结尾不带 `/`）
   - `MAX_UPLOAD_MB` → `100`
   - `USER_PASSCODE` → 第【4】行（想设就设，留空则开放提交）
   - `MIGRATE_SOURCE_URL` → 第【5】行（默认就是线上旧地址，一般不用改）
4. 添加完点保存。
5. 回到项目 **Deployments** → 最新一次构建 → **Redeploy**（重新部署，让新配置和桶绑定生效）。

---

## 第五步：首次上线，无需导入（原数据自动从 GitHub 读取）

> 新架构下**不需要**「导入旧数据」这一步。原始工具始终直接从 GitHub 读取（只读，绝不写回），
> 网站一上线就会显示你原来的 8 个工具；之后你和管理员在平台上「提交 / 审批 / 删除」的新数据全部存 R2。
> （GitHub 偶尔抽风时，会自动用 R2 里缓存的一份兜底，原数据不会丢。）

1. 打开 `https://tools-2ks.pages.dev/` → 右上角「登录」→ `liyt1` + 你设的密码。
2. 登录后右上角出现你的用户名，点它 → 进入「管理面板 / 仪表盘」。
3. 面板里已没有「导入旧数据」按钮（已移除），直接看「待审核」和「已上线」即可。原 GitHub 工具在「已上线」里会标「原·GitHub」徽章，不可删（要删只能去 GitHub 删）。

---

## 第六步：验证上线

1. 打开 `https://tools-2ks.pages.dev/`。
2. 用 `liyt1` 登录 → 应显示「liyt1」和「退出」。
3. 退出后，用随便一个名字（不设 `USER_PASSCODE` 时不用密码）以「普通用户」登录 → 试着提交一个工具 → 它会进入「待审核」。
4. 再用 `liyt1` 管理员登录 → 管理面板里能看到待审核列表 → 点「通过」→ 前台立刻能看到该工具（无需手动刷新缓存）。

---

## 第六步（可选）：给工具配图标

> 现在**不填图标也会有图标**：只要工具填了「访问/下载地址」，网站会自动按该网址去抓站点图标并显示。
> 想固定成自己想要的图标，用下面任一种方式。

**方式 A · 自动获取（推荐）**
1. 提交工具时，先填好「访问/下载地址」→ 点图标框右侧的 **🔍 自动获取** → 图标地址和预览立刻出现 → 正常提交。
2. 已经上线的工具补图标：管理员登录 → 管理后台 →「已上线」→ 点该工具的 **🖼️ 图标** → **按网址自动获取** → **保存**。

**方式 B · 上传本地图片**（适合没有官网的 EXE 工具，如本地小工具）
1. 提交表单里「图标」下方选一张 png/jpg/svg/webp/ico 图片（≤1MB）；
2. 或在后台「🖼️ 图标」面板里点 **⬆️ 上传本地图片**。

**方式 C · 手填图片地址**：在图标地址框里直接贴一个图片网址即可。

**说明**：图标由 Cloudflare 后端抓取后存进 R2，通过本站地址 `/api/icon/...` 访问 —— 国内加载稳定，对方网站改版也不会失效；同一网址只抓一次。若抓取失败（比如目标站没有 favicon），会自动退回显示工具名的**首字母**，不会开天窗。

---

## 第七步（常用）：修改已上线的工具

> 名称写错、描述要改、网址失效、程序包更新版本，都用这一步，**不需要删掉重提**。

1. 用管理员账号登录 → 打开**管理后台** → 「**已上线**」；
2. 找到要改的工具 → 点 **✏️ 编辑**；
3. 在弹窗里改你要改的内容（名称 / 类型 / 描述 / 标签 / 作者 / 网址 / 图标 / 程序包）→ **保存修改**，立即生效。

**几个要注意的点**

- **换程序包**：在「替换程序包」里选新文件即可，旧文件会自动清理。不选的话保持原来的包不变。
- **换图标**：可以直接上传图片，或按网址重新点 **🔍 自动获取**。原来的图标会自动清理。
- **标着「原·GitHub」的工具改不了**，后台里这类工具只显示「只读」——它们的数据在你的 GitHub 仓库里，平台设计上不去改动。要改的话只能改 GitHub 上的 `tools.json` 原文（改完后网站会自动读到）。
- **网址和程序包至少要留一个**，两个都清空会保存失败。
- 只有**管理员**能看到编辑按钮。程序包单个上限由环境变量 `MAX_UPLOAD_MB` 决定（默认 100MB）。

---

## 回滚（万一新版本出问题）

- GitHub 仓库页面 → **Commits** → 找到「升级」那条 → 点 **Revert**（撤销）或 `<>` 切回旧版本，再推送；Cloudflare 会自动重新部署回旧版。
- 旧数据（`tools.json` 里原有工具）已在 git 历史里备份，不会被破坏。

---

## 常见问题

**Q：登录提示「管理员密钥错误」？**
A：密码填错了，或第一步哈希没生成对（重新用 gen-admin.html 生成一次，确保复制的是整行）。

**Q：提交 / 审批提示 R2 错误？**
A：九成是 `TOOLS_BUCKET` 绑定名写错了（必须一字不差叫 `TOOLS_BUCKET`），或 `R2_PUBLIC_BASE` / `MAX_UPLOAD_MB` 没填。看 Cloudflare 里 Functions 的日志（项目 → Logs）。

**Q：上线后前台空白 / 看不到旧工具？**
A：原数据直接从 GitHub 读取（只读）。看不到时：① 检查你旧仓库 `zounvshi/tools` 的 `tools.json` 是否还在、是否公开；② 若 GitHub 暂时抽风，网站会用 R2 缓存兜底，稍后自动恢复；③ 若连 R2 也空且 GitHub 挂了，只会显示你新提交的数据。常规排查仍看 `TOOLS_BUCKET` 绑定和桶是否存在。

**Q：我想用自己的仓库而不是 zounvshi/tools？**
A：把代码推到你自己的仓库，然后在 Cloudflare 环境变量里把 `MIGRATE_SOURCE_URL` 改成你旧仓库的 `tools.json` 公开 raw 地址即可（其余不用改）。原数据仍然是从你这个旧地址**实时读取**，不会被改动。

**Q：普通用户提交要不要密码？**
A：`USER_PASSCODE` 留空 = 只需填个名字就能提交（适合内部分享）；填了 = 大家要用同一个口令才能提交（防乱提交）。

**Q：点了「自动获取图标」提示获取失败？**
A：三种常见原因：① 地址栏填的是 `raw.githubusercontent.com` / `r2.dev` 这类**下载直链**而不是工具官网（这类域名被有意跳过，避免所有工具都显示成 GitHub 图标）—— 请改用「上传本地图片」；② 目标站点确实没有 favicon；③ 目标站在境外且 Cloudflare 也连不上。前两种直接上传一张 png 即可。

**Q：图标为什么不直接引用第三方 favicon 服务？**
A：实测 Google、DuckDuckGo、favicon.im 在国内网络基本不通，引用了也是空白。现在改为后端抓取后存进你的 R2，再用本站域名返回，国内稳定且永久有效。
